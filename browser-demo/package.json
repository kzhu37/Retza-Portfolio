{
  "name": "retza-browser-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.node-test.js",
    "build": "node build.mjs",
    "verify": "npm test && npm run build"
  }
}
