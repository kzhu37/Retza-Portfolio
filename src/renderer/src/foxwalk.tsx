import React from 'react'
import ReactDOM from 'react-dom/client'
import FoxWalkAnimation from './components/FoxWalkAnimation'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <FoxWalkAnimation />
  </React.StrictMode>
)
